# Dev: leave empty — requests go to /api on the same host as the UI and Vite proxies to :8787 (no CORS).
# If you set a full URL here, the backend must allow your browser Origin (dev: any http(s) origin is allowed).
# Production: https://your-api.example.com/api/
VITE_API_URL=
